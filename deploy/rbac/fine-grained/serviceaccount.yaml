# Fine-grained: the ServiceAccount lives in the target namespace and is granted a
# namespaced Role only — the grant cannot leak to other namespaces.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-scoped
  namespace: team-a
